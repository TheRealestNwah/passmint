{
  "name": "passmint",
  "version": "1.2.1",
  "description": "Firefox extension that generates strong passwords and passphrases with fine-grained complexity controls.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "homepage": "https://github.com/TheRealestNwah/passmint",
  "repository": {
    "type": "git",
    "url": "https://github.com/TheRealestNwah/passmint.git"
  },
  "scripts": {
    "test": "node --test test/generator.test.js test/theme.test.js test/appearance.test.js",
    "test:popup": "node --test test/popup.test.js",
    "lint": "web-ext lint --source-dir=. --ignore-files=test test/** node_modules/** web-ext-artifacts/** .claude/**",
    "start": "web-ext run --source-dir=. --ignore-files=test test/** node_modules/** web-ext-artifacts/** .claude/**",
    "build": "web-ext build --source-dir=. --overwrite-dest --ignore-files=test test/** node_modules/** web-ext-artifacts/** .claude/** package*.json",
    "sign": "web-ext sign --source-dir=. --ignore-files=test test/** node_modules/** web-ext-artifacts/** .claude/** package*.json",
    "check-versions": "node .github/scripts/check-versions.mjs"
  },
  "devDependencies": {
    "playwright": "1.56.1",
    "web-ext": "^8.3.0"
  },
  "engines": {
    "node": ">=18"
  }
}
